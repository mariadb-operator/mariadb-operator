apiVersion: mariadb.mmontes.io/v1alpha1
kind: Grant
metadata:
  name: user
spec:
  mariaDbRef:
    name: mariadb
  privileges:
    - "SELECT"
    - "INSERT"
    - "UPDATE"
  database: "*"
  table: "*"
  username: user
  grantOption: true
