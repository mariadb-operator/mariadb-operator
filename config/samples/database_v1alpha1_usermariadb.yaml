apiVersion: database.mmontes.io/v1alpha1
kind: UserMariaDB
metadata:
  name: exporter
spec:
  mariaDbRef:
    name: mariadb
  passwordSecretKeyRef:
    name: exporter
    key: password
  maxUserConnections: 20
