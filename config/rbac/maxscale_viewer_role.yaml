# permissions for end users to view maxscales.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: maxscale-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: mariadb-operator-enterprise
    app.kubernetes.io/part-of: mariadb-operator-enterprise
    app.kubernetes.io/managed-by: kustomize
  name: maxscale-viewer-role
rules:
- apiGroups:
  - mariadb.mmontes.io
  resources:
  - maxscales
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - mariadb.mmontes.io
  resources:
  - maxscales/status
  verbs:
  - get
