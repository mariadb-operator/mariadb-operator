apiVersion: k8s.mariadb.com/v1alpha1
kind: Database
metadata:
  name: app
  namespace: app
spec:
  mariaDbRef:
    name: mariadb
    namespace: mariadb
  characterSet: utf8
  collate: utf8_general_ci
