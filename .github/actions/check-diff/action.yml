name: "Check diff"

description: "Checks if the current branch is clean and errors otherwise"

inputs:
  message:
    description: "Error message being used if changes are detected"
    required: true
    default: "Changes detected"
runs:
  using: "composite"
  steps:
    - name: Error if changes detected
      shell: bash
      run: |
        if [[ `git status --porcelain` ]]; then
          echo "${{ inputs.message }}"
          exit 1
        fi
