package resources

var (
	// AgentPortName is the name of the port where the Agent API is exposed
	AgentPortName = "agent"
	// AgentPortName is the name of the port where the Agent probe API is exposed
	AgentProbePortName = "agent-probe"
)
